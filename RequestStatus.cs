namespace NationStatesSharp
{
    public enum RequestStatus
    {
        Pending,
        Success,
        Canceled,
        Failed
    }

    public enum ResponseFormat
    {
        Xml,
        Boolean
    }
}